## 1.0.0

- Initial release
- `SentryCrashReporter` — Error and message capture via Sentry
- `SentryLoggerProvider` — Structured logging with breadcrumbs and error capture
- `SentryProviderBundle` — Convenience grouping for all Sentry providers
