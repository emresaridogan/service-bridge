name: service_bridge_sentry
description: Sentry provider implementations for service_bridge.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  service_bridge:
    path: ../service_bridge
  sentry_flutter: 9.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: 1.0.5
 